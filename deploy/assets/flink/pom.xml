<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.amazonaws.iov</groupId>
  <artifactId>iov-telemetry-iceberg</artifactId>
  <version>1.0</version>
  <packaging>jar</packaging>

  <!--
    依赖组合参照 AWS 官方示例
    aws-samples/amazon-managed-service-for-apache-flink-examples (java/Iceberg)，
    在其基础上补充 Kafka connector 与 MSK IAM 认证。

    关键点：
      - Flink 自身与 table planner 由 Managed Flink 的 flink-dist 提供，必须是 provided，
        否则会与运行时冲突。
      - iceberg-aws-bundle 提供 Iceberg 需要的 AWS SDK v2 客户端（S3/Glue/STS/KMS），
        避免自己逐个拼 SDK 模块。
      - flink-s3-fs-hadoop 打进 fat jar 是官方示例的做法：Iceberg 的 Parquet 写入
        路径需要 Hadoop Configuration 类。
  -->
  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>11</maven.compiler.source>
    <maven.compiler.target>11</maven.compiler.target>
    <flink.version>1.20.0</flink.version>
    <flink.major.version>1.20</flink.major.version>
    <iceberg.version>1.9.2</iceberg.version>
    <kafka.connector.version>3.4.0-1.20</kafka.connector.version>
    <msk.iam.auth.version>2.3.2</msk.iam.auth.version>
    <kda.runtime.version>1.2.0</kda.runtime.version>
    <log4j.version>2.23.1</log4j.version>
    <slf4j.version>1.7.36</slf4j.version>
    <hadoop.version>3.3.6</hadoop.version>
    <junit.version>5.10.2</junit.version>
  </properties>

  <dependencies>
    <!-- ============ Managed Flink 运行时已提供，不打包 ============ -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-streaming-java</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-clients</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-api-java-bridge</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-runtime</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-table-planner-loader</artifactId>
      <version>${flink.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>com.amazonaws</groupId>
      <artifactId>aws-kinesisanalytics-runtime</artifactId>
      <version>${kda.runtime.version}</version>
      <scope>provided</scope>
    </dependency>
    <!-- Flink 运行时自带 slf4j-api，仅编译期需要（shade 阶段也会排除 org.slf4j:*） -->
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-api</artifactId>
      <version>${slf4j.version}</version>
      <scope>provided</scope>
    </dependency>

    <!-- ============ 需要打进 fat jar ============ -->
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-connector-kafka</artifactId>
      <version>${kafka.connector.version}</version>
    </dependency>
    <dependency>
      <groupId>software.amazon.msk</groupId>
      <artifactId>aws-msk-iam-auth</artifactId>
      <version>${msk.iam.auth.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-json</artifactId>
      <version>${flink.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.iceberg</groupId>
      <artifactId>iceberg-flink-runtime-${flink.major.version}</artifactId>
      <version>${iceberg.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.iceberg</groupId>
      <artifactId>iceberg-aws-bundle</artifactId>
      <version>${iceberg.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.flink</groupId>
      <artifactId>flink-s3-fs-hadoop</artifactId>
      <version>${flink.version}</version>
    </dependency>

    <!--
      Iceberg 的 Flink catalog 工厂需要【未经 relocate】的
      org.apache.hadoop.conf.Configuration：
        FlinkCatalogFactory.createCatalog -> clusterHadoopConf()

      flink-s3-fs-hadoop 里虽然带了 Hadoop，但它把包名重定位到了
      org.apache.flink.fs.shaded.hadoop3.* 之下，因此并不能满足这个引用。
      缺少它的表现是应用启动时抛
        NoClassDefFoundError: org/apache/hadoop/conf/Configuration
      并退回 READY 状态。

      注意：AWS 官方 Iceberg 示例用的是 DataStream sink，那条路径不经过
      clusterHadoopConf()，所以它的 pom 里没有这两个依赖；
      改用 Table API / SQL 的 CREATE CATALOG 之后就必须补上。

      选 hadoop-client-* 而不是 hadoop-common：前者是两个自包含的 uber jar，
      第三方依赖已在内部 shade，不会把 guava/jetty/jackson 等一大堆传递依赖
      拖进来与 Flink 自带版本冲突。
    -->
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-client-api</artifactId>
      <version>${hadoop.version}</version>
    </dependency>
    <dependency>
      <groupId>org.apache.hadoop</groupId>
      <artifactId>hadoop-client-runtime</artifactId>
      <version>${hadoop.version}</version>
      <scope>runtime</scope>
    </dependency>

    <!-- ============ 日志 ============ -->
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-slf4j-impl</artifactId>
      <version>${log4j.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-api</artifactId>
      <version>${log4j.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.apache.logging.log4j</groupId>
      <artifactId>log4j-core</artifactId>
      <version>${log4j.version}</version>
      <scope>runtime</scope>
    </dependency>
    <!-- ============ 测试 ============ -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <finalName>iov-telemetry-iceberg</finalName>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <source>${maven.compiler.source}</source>
          <target>${maven.compiler.target}</target>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.2.5</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.3</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <artifactSet>
                <excludes>
                  <exclude>org.apache.flink:force-shading</exclude>
                  <exclude>com.google.code.findbugs:jsr305</exclude>
                  <exclude>org.slf4j:*</exclude>
                  <exclude>log4j:*</exclude>
                </excludes>
              </artifactSet>
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                    <exclude>module-info.class</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers>
                <!-- connector / filesystem 的 SPI 文件必须合并，否则找不到 factory -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>com.amazonaws.iov.TelemetryToIcebergJob</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
